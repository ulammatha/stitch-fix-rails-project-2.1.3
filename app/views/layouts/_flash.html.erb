<div class='col-xs-12'>
  <%- flash.each do |type, message| %>
    <div class="alert <%= bootstrap_class_for(type.to_sym) %>" role="alert" id="clearance_error">
      <button class="close">x</button>
      <%= message %>
    </div>
  <% end %>
</div>
